spring:
  application:
    name: account
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
        shared-configs:
          - data-id: spring-boot.yml
          - data-id: spring-mybatis.yml
          - data-id: spring-redis.yml
          - data-id: link-account.yml
        group: short_link